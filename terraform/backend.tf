# Remote backend configuration is intentionally out of scope for this demo.
#
# Uncomment and adapt this local-state example only for disposable learning environments.
#
# terraform {
#   backend "local" {
#     path = "terraform.tfstate"
#   }
# }
